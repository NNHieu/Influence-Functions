wandb:
  # _target_: pytorch_lightning.loggers.wandb.WandbLogger
  _target_: utils.wandb.CustomWandbLogger

  name: ${datamodule.dataset_name}_${datamodule.flip_percent}_${net.name}_${seed}_${now:%Y-%m-%d}_${now:%H-%M-%S} # name of the run (normally generated by wandb)
  save_dir: "${paths.output_dir}"
  offline: False
  id: null # pass correct id to resume experiment!
  anonymous: null # enable anonymous logging
  project: "error_tracing_image_1452"
  log_model: True # upload lightning ckpts
  prefix: "" # a string to put at the beginning of metric keys
  # entity: "" # set to name of your wandb team
  group: ""
  tags: []
  job_type: ""